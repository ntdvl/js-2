var str = 'A string is a data type used in programming, such as an integer and floating point unit, but is used to represent text rather than numbers.'

var str2 = str.toLocaleLowerCase();

var str3 = str.toLocaleUpperCase();

console.log(str2);
console.log(str3);